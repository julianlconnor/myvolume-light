require.config({
    /*
    * Base url points to root of project.
    */
    baseUrl: '/javascripts/',
    /*
    * Main is the first file inside of base url that should be loaded.
    */
    deps: ["app/main"]
});

